/*
 * *Created by NetaloTeamAndroid on 2020
 * Company: Netacom.
 *  *
 */

package com.mytel.android

import com.nvilabs.sdk.core.BaseSDKApplication

/**Created by vantoan on 23,July,2020
Company: Netacom.
Email: huynhvantoan.itc@gmail.com
 **/

class MyTelApplication : BaseSDKApplication() {

    override fun onCreate() {
        super.onCreate()
        /*val neTheme = NeTheme(
            mainColor = ColorDefine.BLUE_MAIN_COLOR,
            subColorLight = ColorDefine.BLUE_SUB_COLOR_LIGHT,
            subColorDark = ColorDefine.BLUE_SUB_COLOR_DARK,
            toolbarDrawable = this.resNameById(R.drawable.bg_header)
        )
        NetAloSDK.initNetAloSDK(
            context = this,
            netAloSdkCore = netAloSdkCore,
            sdkConfig = SdkConfig(
                appId = AppID.VNDIRECT,
                appKey = AppKey.VNDIRECT_DEV,
                accountKey = AccountKey.VNDIRECT_DEV
            ),
            neTheme = preferencesHelperImpl.neTheme ?: neTheme
        )
        initTracking()
        initTest()*/
    }
}
